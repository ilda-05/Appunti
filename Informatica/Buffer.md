
Un buffer è un spazio di memoria temporaneo dove vengono inseriti dati in attesa per essere usati.

Solitamente i dati del buffer vengono presi da input o prima di essere mandati a dispositivi di output.

Un buffer può essere anche usato tra i processi della stessa macchina.